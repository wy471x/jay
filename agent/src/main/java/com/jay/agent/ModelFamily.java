package com.jay.agent;

/**
 * High-level model family used for shared identity affordances across clients.
 * Equivalent to Rust's ModelFamily enum.
 */
public enum ModelFamily {
    DEEPSEEK,
    ANTHROPIC,
    OPENAI,
    GOOGLE,
    META,
    MISTRAL,
    QWEN,
    GROK,
    COHERE,
    GPT_OSS,
    INFERENCER
}
